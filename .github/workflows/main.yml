name: Kotlin Dependency License Check

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  check-licenses:
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Determine the latest Go version dynamically
      - name: Determine latest Go version
        id: get_go_version
        run: |
          latest_go_version=$(curl -s https://go.dev/VERSION?m=text | sed 's/^go//' | head -n 1)
          echo "go-version=$latest_go_version" >> "$GITHUB_OUTPUT"

      # Step 3: Set up Go environment
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ steps.get_go_version.outputs.go-version }}

      # Step 4: Create a directory for the license checker
      - name: Create license checker directory
        run: mkdir -p license-checker

      # Step 5: Build and run the license checker
      - name: Build and run license checker
        shell: bash
        run: |
          cd license-checker

          # Add Go code for the license checker
          cat <<-'EOF' > license-checker.go
          package main

          import (
            "encoding/xml"
            "fmt"
            "io/fs"
            "io/ioutil"
            "net/http"
            "os"
            "path/filepath"
            "strings"

            "github.com/pelletier/go-toml"
          )

          type License struct {
            Name string `xml:"name"`
            URL  string `xml:"url"`
          }

          type MavenPOM struct {
            XMLName  xml.Name  `xml:"project"`
            Licenses []License `xml:"licenses>license"`
          }

          // Find all .toml files recursively in the directory
          func findTOMLFiles(rootDir string) ([]string, error) {
            var tomlFiles []string
            err := filepath.WalkDir(rootDir, func(path string, d fs.DirEntry, err error) error {
              if err != nil {
                return err
              }
              if !d.IsDir() && strings.HasSuffix(d.Name(), ".toml") {
                tomlFiles = append(tomlFiles, path)
              }
              return nil
            })
            return tomlFiles, err
          }

          // Parse a single .toml file to extract dependencies
          func parseTOMLFile(filePath string) (map[string]string, error) {
            dependencies := make(map[string]string)

            tree, err := toml.LoadFile(filePath)
            if err != nil {
              return nil, fmt.Errorf("error loading TOML file: %v", err)
            }

            versions := tree.Get("versions").(*toml.Tree)
            libraries := tree.Get("libraries").(*toml.Tree)

            for _, value := range libraries.ToMap() {
              lib := value.(map[string]interface{})
              module := lib["module"].(string)
              versionRef := lib["version.ref"].(string)
              dependencies[module] = versions.Get(versionRef).(string)
            }

            return dependencies, nil
          }

          // Fetch POM file from Maven repository
          func fetchPOM(url string) ([]byte, error) {
            resp, err := http.Get(url)
            if err != nil {
              return nil, err
            }
            defer resp.Body.Close()
            return ioutil.ReadAll(resp.Body)
          }

          // Parse POM XML data
          func parsePOM(data []byte) (MavenPOM, error) {
            var pom MavenPOM
            err := xml.Unmarshal(data, &pom)
            return pom, err
          }

          // Get license information for a dependency
          func getLicenseInfo(dependency string, version string) (string, string, string) {
            url := fmt.Sprintf("https://repo1.maven.org/maven2/%s/%s/%s/%s-%s.pom", strings.ReplaceAll(dependency, ".", "/"), dependency, version, dependency, version)
            data, err := fetchPOM(url)
            if err == nil {
              pom, err := parsePOM(data)
              if err == nil && len(pom.Licenses) > 0 {
                return pom.Licenses[0].Name, pom.Licenses[0].URL, ""
              }
            }
            searchURL := fmt.Sprintf("https://www.google.com/search?q=%s+%s+maven+repository", url.PathEscape(dependency), url.PathEscape(version))
            return "Unknown", "#", searchURL
          }

          // Generate an HTML report for dependencies
          func generateHTMLReport(dependencies map[string]string) {
            html := `
            <!DOCTYPE html>
            <html>
            <head>
                <title>Kotlin Dependency License Report</title>
                <style>
                    table { width: 100%; border-collapse: collapse; }
                    th, td { border: 1px solid #ddd; padding: 8px; }
                    th { background-color: #f2f2f2; }
                </style>
            </head>
            <body>
                <h1>Kotlin Dependency License Report</h1>
                <table>
                    <tr><th>Dependency</th><th>Version</th><th>License</th><th>License URL</th><th>Search Link</th></tr>`

            for dep, version := range dependencies {
                licenseName, licenseURL, searchLink := getLicenseInfo(dep, version)
                html += fmt.Sprintf("<tr><td>%s</td><td>%s</td><td>%s</td><td><a href='%s' target='_blank'>%s</a></td><td><a href='%s' target='_blank'>Search</a></td></tr>", dep, version, licenseName, licenseURL, licenseURL, searchLink)
            }

            html += `
                </table>
            </body>
            </html>`

            ioutil.WriteFile("license-report.html", []byte(html), 0644)
          }

          func main() {
            rootDir := os.Getenv("GITHUB_WORKSPACE")
            tomlFiles, err := findTOMLFiles(rootDir)
            if err != nil {
              fmt.Println("Error finding TOML files:", err)
              return
            }
            if len(tomlFiles) == 0 {
              fmt.Println("No TOML files found. Exiting.")
              return
            }

            dependencies := make(map[string]string)
            for _, file := range tomlFiles {
              deps, err := parseTOMLFile(file)
              if err != nil {
                fmt.Println("Error parsing TOML file:", file, err)
                continue
              }
              for k, v := range deps {
                dependencies[k] = v
              }
            }
            generateHTMLReport(dependencies)
            fmt.Println("License report generated: license-report.html")
          }
          EOF

          # Initialize Go module and fetch dependencies
          go mod init license-checker
          go mod tidy
          go build -o license-checker
          cd ..

          # Execute the license checker
          if ! ./license-checker/license-checker; then
            echo "Error: license-checker encountered an issue."
            exit 1
          fi

      # Step 6: Upload the license report as an artifact
      - name: Upload license report artifact
        uses: actions/upload-artifact@v4
        with:
          name: license-report
          path: license-checker/license-report.html

      # Step 7: Upload the binary artifact of the license checker
      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: kotlin-license-checker
          path: license-checker/license-checker
